<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="utez.edu.mx.servicios">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity
            android:name=".User.UserMenu.MenuUserActivity"
            android:exported="true"
            android:label="@string/title_activity_menu_user"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".ServiceProvider.SingupServiceProviderActivity"
            android:exported="true" />
        <activity
            android:name=".User.SignupUserScreen"
            android:exported="true" />
        <activity
            android:name=".ServiceProvider.LoginServiceProviderScreen"
            android:exported="true" />
        <activity
            android:name=".User.LoginUserScreen"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>